@model List<AlquilerVehiculoP2.Models.facturas>

@{
    ViewBag.Title = "Dashboard";
}

<meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate" />
<meta http-equiv="Pragma" content="no-cache" />
<meta http-equiv="Expires" content="0" />

<h2>Dashboard</h2>

<div>
    <!-- Aquí puedes agregar las opciones del dashboard para cada una de tus tablas -->
    <ul>
        <li>@Html.ActionLink("Empleados", "Empleados", "Home")</li>
        <li>@Html.ActionLink("Vehículos", "Vehiculos", "Home")</li>
        <li>@Html.ActionLink("Facturas", "Facturas", "Home")</li>
        <!-- Agrega más opciones según tus tablas -->
    </ul>
</div>

<div>
    <!-- Aquí puedes mostrar los datos de las facturas -->
    <h3>Alquileres</h3>
    <table>
        <thead>
            <tr>
                <th>Nombre</th>
                <th>Cédula</th>
                <!-- Agrega más columnas según tus necesidades -->
            </tr>
        </thead>
        <tbody>
            @foreach (var factura in Model)
            {
                <tr>
                    <td>@factura.NombreCliente</td>
                    <td>@factura.CedulaCliente</td>
                    <!-- Agrega más columnas según tus necesidades -->
                </tr>
            }
        </tbody>
    </table>
</div>